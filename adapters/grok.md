# dev-spec-flow Project Adapter For Grok

Grok project-instruction locations and advanced agent capabilities must be discovered from the
actual client environment. Do not copy this file to a guessed path. Merge the relevant body into a
verified project instruction surface, or use it as an explicit `--dest` installation template.

## Project Sources Of Truth

- Architecture: `<repository-relative architecture path>`
- Documentation index: `<repository-relative documentation index>`
- Current behavior: `openspec/specs/`
- Active changes: `openspec/changes/`

## Validation

- Project checks: `<verified project command>`
- Change readiness: `python <skill-root>/scripts/dev_spec_flow.py --root . verify --change <slug> --readiness`
- Documentation checks: `<verified docs-architect command, when configured>`

## Constraints

- Use the canonical `dev-spec-flow` Skill; do not maintain a copied Grok-specific workflow.
- Probe available parallelism, subagents, browsing, and documentation tools before choosing an execution strategy.
- Fall back to the same review charters sequentially when isolation or parallel execution is unavailable.
- Update affected current docs and close the active change before claiming completion.
